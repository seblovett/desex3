
for dir in ./*/
do
	dir=${dir%*/}
	echo ${dir##*/}
	if [ ".git" != ${dir} ]; then  
	if [ "global" != ${dir##*/} ]; then
		cd ${dir}
		check_magic_leaf_cell ${dir##*/}
		#read -p "Press [Enter] key to continue"
		cd ..
	fi
	fi
done



