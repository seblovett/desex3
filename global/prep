mkdir -p ~/design/magic/c035u/cell_lib
for dir in ./*/
do
	dir=${dir%*/}
	echo ${dir##*/}
	if [ ".git" != ${dir} ]; then  
		cp ${dir}/${dir}.mag ~/design/magic/c035u/cell_lib/${dir}.mag
	fi
done
cp global/all.mag ~/design/magic/c035u/cell_lib/all.mag

**prepare_dicd desex3 < global/prepinput



